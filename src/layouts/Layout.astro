---
/**
 * @deprecated Use BaseLayout.astro directly. Kept for backward compatibility.
 */
import BaseLayout from './BaseLayout.astro';

interface Props {
	seo?: import('@/types/seo').SeoOptions | import('@/types/seo').PageMetadata;
	lang?: string;
	class?: string;
}

const props = Astro.props;
---

<BaseLayout {...props}>
	<slot />
</BaseLayout>
